services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - FASTAPI_HOST=0.0.0.0
      - FASTAPI_PORT=8080
      - DEBUG=False
    volumes:
      - ./scripts:/app/scripts
      - ./src/backend:/app/src/backend
    command: python scripts/run_backend.py